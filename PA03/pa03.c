#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include"answer03.h"

int main(int argc, char * * argv)
{
  //char * arr = malloc(sizeof(char) * 18);
  //strcpy(arr,"a big long string");

  //char * arr2 = make_substr(arr,2,3);
  //printf("string = %s\n", arr2);
 
  return EXIT_SUCCESS;
}
/*
char * make_substr(const char * s, int start_ind, int len)
{
  //LOCAL DECLARATIONS                                                                                                                                                                                                                       
  char * buffer = malloc(sizeof(char) * (len + 1));
  memcpy(buffer,s,len);
  buffer[len + 1] = '\0';

  return buffer;
}
*/

